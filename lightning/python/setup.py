from setuptools import setup, find_packages

setup(
    name="lightning",
    version="0.1.0",
    description="Rust Lightning QUIC Client - Python bindings",
    packages=find_packages(),
    python_requires=">=3.8",
    install_requires=[],
)
